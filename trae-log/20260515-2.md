## 37345
### prompt
```
1. 工作目录为D:\code-1\xiaoyi-trae\eiker-be\docs
2. 根据全局规则
   1) 任务开始前：git fetch & 创建新feature分支，来源分支为origin/feature/company_design_20260515_2
   2) 任务完成后：git commit & git push
3. 任务实体（企业名称-拉新）设计，根据以下企业名称服务场景设计任务实体列表
   1) 任务类型: 基础拉新、用户拉新、业务拉新、公司拉新
   2) 任务的输入：
      A. 任务数据：任选其一
         a. 文件(.sql, .csv, .xlsx, .json, .txt, .xml)
         b. 图片(.png, .jpg, .bmp)
         c. 压缩包(.zip, .rar, .tar, .gz)
         d. 文件oss路径
      B. 任务数据验证规则列表
         a. 如果没有传入，则取任务类型上配置的规则列表
   3) 任务处理step，每一步都需要对输入、输出进行留痕（落postgresql表）
4. 任务处理step
   1) 文件读取与解析，需要将解析出来的文件内容记入pg临时表
   2) 数据验证
      A. 错误大类: 完整性验证错误、合规性验证错误、数据重复
      B. 错误小类
         a. 完整性验证错误：统一社会信用代码、企业名称、法定代表人、成立日期、登记状态、住所，任一字段缺失
         b. 合规性验证错误：统一社会信用代码非法、企业名称字符非法、企业名称长度不在[4,26]、法定代表人字符、法定代表人长度不在[2,5]、成立日期非法、登记状态非法
            说明：住所的非法性验证在位置服务处理
         c. 数据重复：同名不同企（本任务）、同名不同企（与数巢）、同企不同名（本任务）、同企不同名（与数巢）
         d. 数据真实性验证：调用天眼查核实输入的企业信息是否正确
      C. 校验准则：根据任务配置的校验规则列表逐条校验，每一条校验结果都需要留痕（落postgresql表）。其中数据真实性验证需要记录api的url、request、response、调用时间
   3) 数据（企业名称）入数巢company_fact
5. 生成文件存放路径为D:\code-1\xiaoyi-trae\eiker-be\docs\superpowers\company-task
```

### 不满意原因
```
产物不满意
1. git操作又没有执行
2. task_main.input_source_type = 5（直接数据），这个是ai自动加的，prompt只定义了4种
3. task_data_temp 缺少行级综合处理状态字段
4. verify_rules 存的大类标签与 data_verify_rule 表脱节
5. third_api_log 与 data_verify_result 缺少直接关联
```

### prompt - cc
```
review这个目录docs\superpowers\company-task的文件，评估是否满足这个设计要求
1. 想少了：设计缺失的功能
2. 想多了：设计过多的问题，即设计远远超出需求要求
设计要求如下
1. 任务实体（企业名称-拉新）设计，根据以下企业名称服务场景设计任务实体列表
   1) 任务类型: 基础拉新、用户拉新、业务拉新、公司拉新
   2) 任务的输入：
      A. 任务数据：任选其一
         a. 文件(.sql, .csv, .xlsx, .json, .txt, .xml)
         b. 图片(.png, .jpg, .bmp)
         c. 压缩包(.zip, .rar, .tar, .gz)
         d. 文件oss路径
      B. 任务数据验证规则列表
         a. 如果没有传入，则取任务类型上配置的规则列表
   3) 任务处理step，每一步都需要对输入、输出进行留痕（落postgresql表）
2. 任务处理step
   1) 文件读取与解析，需要将解析出来的文件内容记入pg临时表
   2) 数据验证
      A. 错误大类: 完整性验证错误、合规性验证错误、数据重复
      B. 错误小类
         a. 完整性验证错误：统一社会信用代码、企业名称、法定代表人、成立日期、登记状态、住所，任一字段缺失
         b. 合规性验证错误：统一社会信用代码非法、企业名称字符非法、企业名称长度不在[4,26]、法定代表人字符、法定代表人长度不在[2,5]、成立日期非法、登记状态非法
            说明：住所的非法性验证在位置服务处理
         c. 数据重复：同名不同企（本任务）、同名不同企（与数巢）、同企不同名（本任务）、同企不同名（与数巢）
         d. 数据真实性验证：调用天眼查核实输入的企业信息是否正确
      C. 校验准则：根据任务配置的校验规则列表逐条校验，每一条校验结果都需要留痕（落postgresql表）。其中数据真实性验证需要记录api的url、request、response、调用时间
   3) 数据（企业名称）入数巢company_fact
```


## 69897
### prompt
```
1. 工作目录为D:\code-1\xiaoyi-trae\eiker-be\docs
2. 根据全局规则
   1) 任务完成后：git commit & git push
3. 任务实体（企业名称-拉新）设计，修复设计bug
   1) 去掉task_main.input_source_type = 5
   2) task_data_temp 增加行级综合处理状态字段
   3) 解决问题：verify_rules 存的大类标签与 data_verify_rule 表脱节
   4) 解决问题：third_api_log 与 data_verify_result 缺少直接关联
4. 生成文件存放路径为D:\code-1\xiaoyi-trae\eiker-be\docs\superpowers\company-task
```

### 不满意原因
```
产物不满意
1. 任务完成后：git commit & git push
   没有执行git操作
2. 以下bug没有修复
   1) task_data_temp 增加行级综合处理状态字段
   2) 解决问题：verify_rules 存的大类标签与 data_verify_rule 表脱节
   3) 解决问题：third_api_log 与 data_verify_result 缺少直接关联
```

### prompt - cc
```
review这个目录docs\superpowers\company-task的文件，评估是否满足这个设计要求
1. 想少了：设计缺失的功能
2. 想多了：设计过多的问题，即设计远远超出需求要求
设计要求如下
1. 任务实体（企业名称-拉新）设计，修复设计bug
   1) 去掉task_main.input_source_type = 5
   2) task_data_temp 增加行级综合处理状态字段
   3) 解决问题：verify_rules 存的大类标签与 data_verify_rule 表脱节
   4) 解决问题：third_api_log 与 data_verify_result 缺少直接关联
```